const Admin = require('../models/admin.model');
const { AppError } = require('../models/error.model');


// Cria um novo admin:
exports.criar_admin = async (req, res, next) => {

}

// Remover o admin atual:
exports.remover_admin = async (req, res, next) => {

}